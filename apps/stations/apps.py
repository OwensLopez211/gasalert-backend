from django.apps import AppConfig

class StationsConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.stations'  # Asegúrate de que este nombre sea correcto
    verbose_name = 'Estaciones'
